# -----------------------------
# Sistema operacional / arquivos temporários
# -----------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# -----------------------------
# Ambiente / Configurações sensíveis
# -----------------------------
.env
.env.backup
.env.production
auth.json

# -----------------------------
# Dependências / Builds
# -----------------------------
/vendor
/node_modules
/public/build
/public/hot
/public/storage               # symlink do storage (muito importante não commitar)

/storage/*.key
/storage/pail                 # Laravel Pail (logs em tempo real)

/bootstrap/compiled.php       # Obsoleto desde Laravel 5, mas por segurança
app/storage/                  # Obsoleto (Laravel 4), mas por segurança

# Variantes se public foi renomeado para public_html (opcional)
public_html/storage
public_html/hot

# -----------------------------
# Ferramentas de dev / IDEs / Editores
# -----------------------------
.phpunit.result.cache
.phpunit.cache/
.phpactor.json

# IDEs comuns
/.idea
/.vscode
/.fleet
/.nova
/.zed

# Vagrant / Homestead (se usar)
Homestead.json
Homestead.yaml
/.vagrant

# Outros caches / temp
.phpactor.json                # Já tem acima, mas reforçando